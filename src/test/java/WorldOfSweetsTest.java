import org.junit.Test;
import static org.junit.Assert.*;
import static org.mockito.Mockito.*;
import java.util.ArrayList;

import sweets.WorldOfSweets;
import sweets.SweetState;
import sweets.Player;

public class WorldOfSweetsTest {

    // Incase anybody hasn't taken 1632, here's how you write a test
    // Also, if you haven't taken 1632, I HIGHLY suggest you do before you graduate.

    /*
    @Test
    public void testThisOrThat() {
        value = "lol hey";
        value2 = "soup bruh";
        assertEquals(value, value2); // This test will fail.
    }


    */

    /*

    @Test
    public void testcheckForPlayerFindPositive() 
    {
       WorldOfSweets testState = mock(WorldOfSweets.class);

       Player player1 = mock(Player.class);
       when(player1.getPlayerX()).thenReturn(900);
       when(player1.getPlayerY()).thenReturn(400);
       
       Player player2 = mock(Player.class);
       when(player2.getPlayerX()).thenReturn(20);
       when(player2.getPlayerY()).thenReturn(800);
       
       Player player3 = mock(Player.class);
       when(player3.getPlayerX()).thenReturn(200);
       when(player3.getPlayerY()).thenReturn(600);

        ArrayList<Player> testList = new ArrayList<Player>();

        testList.add(player1);
        testList.add(player2);
        testList.add(player3);

        
        assertEquals(testState.checkForPlayer(20, 799, testList), 1);
    }

    @Test
    public void testcheckForPlayerFindNegative() 
    {
       WorldOfSweets testState = mock(WorldOfSweets.class);

       Player player1 = mock(Player.class);
       when(player1.getPlayerX()).thenReturn(900);
       when(player1.getPlayerY()).thenReturn(400);
       
       Player player2 = mock(Player.class);
       when(player2.getPlayerX()).thenReturn(20);
       when(player2.getPlayerY()).thenReturn(800);
       
       Player player3 = mock(Player.class);
       when(player3.getPlayerX()).thenReturn(200);
       when(player3.getPlayerY()).thenReturn(600);

        ArrayList<Player> testList = new ArrayList<Player>();

        testList.add(player1);
        testList.add(player2);
        testList.add(player3);

        
        assertEquals(testState.checkForPlayer(1200, 10, testList), 0);
    }
    */
    
}

